{
  "name": "slm-erp-v2",
  "private": true,
  "packageManager": "pnpm@11.14.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "docker compose up -d && turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "db:prepare": "pnpm --filter @repo/api-auth db:prepare -- && pnpm --filter @repo/api-customers db:prepare -- && pnpm --filter @repo/api-invoices db:prepare --",
    "test:e2e:cross": "pnpm build && node e2e/prepare-test-dbs.mjs && jest --config e2e/jest.config.json"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.0",
    "@types/supertest": "^6.0.2",
    "dotenv": "^17.0.0",
    "jest": "^29.7.0",
    "prettier": "^3.5.3",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.6",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  }
}
